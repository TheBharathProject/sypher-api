# SDE-1 DSA Problem List for Interview Preparation

## Arrays and Matrix

### Arrays

1. Pair with Given Sum — *Find two numbers whose sum equals a target value.* citeturn5view0
2. Best Time to Buy and Sell Stock — *Compute the maximum profit from a single buy-sell transaction.* citeturn5view0
3. Maximum Subarray — *Find the contiguous subarray with the largest sum.* citeturn5view0
4. Product of Array Except Self — *Build the output array without using division.* citeturn5view0
5. Find Duplicates in an Array — *Detect repeated values efficiently in an unsorted array.* citeturn8view0
6. 3Sum / Triplet Sum to a Given Value — *Find three values that satisfy a target sum condition.* citeturn8view0
7. First Missing Positive / Smallest Positive Missing Number — *Return the smallest missing positive integer from an unsorted array.* citeturn8view0

### Matrix

1. Transpose of a Matrix — *Swap rows and columns correctly and efficiently.* citeturn5view0
2. Set Matrix Zeroes — *Zero out rows and columns based on zero-valued cells.* citeturn18search0
3. Spiral Matrix — *Traverse a matrix in spiral order.* citeturn5view0
4. Search a 2D Matrix — *Search a row-wise structured matrix using binary search logic.* citeturn18search2
5. Rotate Image — *Rotate an \(n \times n\) matrix in place by 90 degrees.* citeturn18search1
6. Search a Row-wise and Column-wise Sorted Matrix — *Search efficiently in a matrix sorted both row-wise and column-wise.* citeturn8view0

## Strings, Hashing, and Sliding Window

### Strings

1. Valid Anagram — *Check whether two strings are anagrams of each other.* citeturn3view0
2. Longest Common Prefix — *Find the longest shared prefix across a set of strings.* citeturn1view5
3. Roman Number to Integer — *Convert a Roman numeral string to its numeric value.* citeturn8view0
4. String to Integer (atoi) — *Parse a signed integer from a string with edge-case handling.* citeturn8view0
5. Longest Palindromic Substring — *Return the longest palindromic substring in a string.* citeturn5view0
6. Palindromic Substrings — *Count all palindromic substrings in a string.* citeturn5view0

### Hashing and Prefix Sum

1. Contains Duplicate — *Determine whether any value appears more than once.* citeturn3view0
2. Group Anagrams — *Group words that are permutations of the same letters.* citeturn9search2
3. Valid Sudoku — *Validate rows, columns, and sub-boxes using constant-space bookkeeping.* citeturn9search1
4. Longest Consecutive Sequence — *Find the longest run of consecutive values in linear time.* citeturn5view0
5. Subarray Sum Equals K — *Count subarrays whose sum equals a given target.* citeturn9search0
6. Count Subarrays with Given XOR — *Count subarrays whose XOR matches a target value.* citeturn5view0

### Sliding Window and Two Pointers

1. Valid Palindrome — *Check whether a string is a palindrome after ignoring non-alphanumerics.* citeturn3view0
2. Container With Most Water — *Maximise area using two moving pointers.* citeturn5view0
3. Longest Substring Without Repeating Characters — *Find the maximum-length substring with all unique characters.* citeturn13search0
4. Longest Repeating Character Replacement — *Maximise a repeating-character window with at most \(k\) changes.* citeturn13search1
5. Permutation in String — *Detect whether one string contains a permutation of another.* citeturn13search2
6. Sliding Window Maximum — *Return the maximum in every window of size \(k\).* citeturn9search3
7. Minimum Window Substring — *Find the smallest substring containing all required characters.* citeturn13search3

## Linked Lists, Stack, Queue, and Heap

### Linked Lists

1. Reverse Linked List — *Reverse a singly linked list in place.* citeturn8view0
2. Merge Two Sorted Lists — *Merge two sorted linked lists into one sorted list.* citeturn5view0
3. Linked List Cycle / Detect Loop — *Detect whether a linked list contains a cycle.* citeturn5view0
4. Remove Nth Node From End of List — *Delete a node using two-pointer traversal.* citeturn5view0
5. Add Two Numbers — *Add two numbers represented as linked lists.* citeturn8view0
6. Reorder List — *Rearrange a list into alternating front-back order.* citeturn11search3
7. Copy List with Random Pointer / Clone a Linked List — *Deep-copy a linked list with extra random links.* citeturn11search2
8. Merge K Sorted Lists — *Merge multiple sorted linked lists efficiently.* citeturn5view0

### Stack and Queue

1. Valid Parentheses / Balanced Brackets — *Validate nested brackets with stack discipline.* citeturn6view0
2. Implement Queue Using Stacks — *Simulate FIFO behaviour using LIFO containers.* citeturn6view0
3. Min Stack — *Support push, pop, top, and minimum in constant time.* citeturn3view0
4. Next Greater Element — *Find the next larger element for each value.* citeturn6view0
5. Evaluate Reverse Polish Notation — *Evaluate postfix arithmetic expressions correctly.* citeturn3view0
6. Daily Temperatures — *Use a monotonic stack to find waiting days.* citeturn3view0
7. Largest Rectangle in Histogram — *Compute the maximum rectangle area in a histogram.* citeturn5view0

### Heap and Priority Queue

1. K Largest Elements in an Array — *Extract the largest \(k\) values efficiently.* citeturn8view0
2. Top K Frequent Elements — *Return the most frequent values using heap or bucket logic.* citeturn5view0
3. Connect N Ropes with Minimum Cost — *Repeatedly combine the cheapest ropes first.* citeturn8view0
4. Find Median from Data Stream — *Maintain running medians as numbers arrive.* citeturn6view0
5. Kth Largest Element in a Stream — *Track the kth largest value online.* citeturn6view0
6. Nearly Sorted Array — *Sort an array where each element is close to its final position.* citeturn6view0

## Trees, BST, and Trie

### Binary Trees

1. Maximum Depth of Binary Tree — *Compute the height of a binary tree.* citeturn21search2
2. Invert Binary Tree — *Mirror a binary tree recursively or iteratively.* citeturn1view5
3. Binary Tree Level Order Traversal — *Traverse nodes level by level.* citeturn1view5
4. Diameter of Binary Tree — *Find the longest path between any two nodes.* citeturn11search0
5. Binary Tree Right Side View — *Return the nodes visible from the right side.* citeturn11search1
6. Binary Tree Maximum Path Sum — *Find the maximum sum over all valid tree paths.* citeturn12search3

### BST and Tree Construction

1. Subtree of Another Tree — *Check whether one tree appears as a subtree inside another.* citeturn5view0
2. Validate Binary Search Tree — *Verify whether a binary tree satisfies BST ordering rules.* citeturn21search3
3. Kth Smallest Element in a BST — *Use inorder properties to find the kth smallest value.* citeturn5view0
4. Lowest Common Ancestor of a Binary Tree — *Find the deepest shared ancestor of two nodes.* citeturn8view0
5. Construct Binary Tree from Preorder and Inorder Traversal — *Rebuild the original tree from traversal arrays.* citeturn12search1
6. Serialize and Deserialize Binary Tree — *Encode and reconstruct a binary tree losslessly.* citeturn12search0

### Trie

1. Implement Trie (Prefix Tree) — *Support insert, search, and prefix operations efficiently.* citeturn5view0
2. Design Add and Search Words Data Structure — *Support wildcard searches over inserted words.* citeturn3view0
3. Longest Common Prefix Using Trie — *Use trie structure to compute the shared prefix.* citeturn6view0
4. Print Unique Rows in a Binary Matrix — *Use trie-like deduplication over matrix rows.* citeturn6view0
5. Palindrome Pairs — *Find all pairs of words that form palindromes when concatenated.* citeturn6view0
6. Word Search II — *Use trie + DFS to find many words on a grid.* citeturn12search2

## Searching, Sorting, and Intervals

### Searching and Binary Search

1. Binary Search — *Search a sorted array in logarithmic time.* citeturn19search0
2. Square Root of an Integer — *Find the floor value of an integer square root.* citeturn8view0
3. First and Last Position of Element in Sorted Array — *Locate the start and end of a target range.* citeturn19search1
4. Search in Rotated Sorted Array — *Search a rotated sorted array in logarithmic time.* citeturn19search3
5. Find Peak Element — *Return any index that is strictly larger than its neighbours.* citeturn19search2
6. Allocate Minimum Pages — *Binary-search the answer over feasible workload limits.* citeturn8view0
7. Median of Two Sorted Arrays — *Compute the joint median without fully merging the arrays.* citeturn8view0

### Sorting and Intervals

1. Sort 0s, 1s, and 2s / Sort Colors — *Sort three categories in one pass.* citeturn20search3
2. Merge Two Sorted Arrays Without Extra Space — *Merge sorted arrays while minimising auxiliary space.* citeturn5view0
3. Count Inversions — *Count out-of-order pairs efficiently with divide-and-conquer.* citeturn5view0
4. Merge Intervals — *Merge overlapping intervals into disjoint ranges.* citeturn5view0
5. Insert Interval — *Insert and merge a new interval into a sorted interval list.* citeturn20search1
6. Non-overlapping Intervals — *Remove the fewest intervals to eliminate overlap.* citeturn20search2
7. Minimum Platforms — *Compute the minimum number of platforms required for train schedules.* citeturn8view0

## Graphs and Backtracking

### Graphs

1. Flood Fill Algorithm — *Recolour or relabel connected cells from a starting point.* citeturn5view0
2. BFS of Graph — *Traverse a graph level by level from a source node.* citeturn8view0
3. DFS of Graph — *Traverse deeply using recursion or an explicit stack.* citeturn8view0
4. Number of Islands — *Count connected components in a grid.* citeturn8view0
5. Clone Graph — *Create a deep copy of a graph with cycles.* citeturn16search3
6. Course Schedule — *Detect whether prerequisite constraints permit completion of all courses.* citeturn5view0
7. Topological Sort — *Produce a valid ordering in a DAG.* citeturn8view0
8. Dijkstra Algorithm — *Find shortest paths from a source in a weighted graph.* citeturn8view0
9. Alien Dictionary — *Infer character ordering from a sorted alien-word list.* citeturn8view0

### Backtracking and Recursion

1. Generate Parentheses — *Generate all valid parenthesis combinations of size \(n\).* citeturn15search0
2. Subsets — *Generate the power set of a list of unique elements.* citeturn15search1
3. Permutations — *Generate all orderings of distinct values.* citeturn15search2
4. Combination Sum — *Find all combinations that add up to a target.* citeturn16search0
5. Word Search — *Determine whether a word can be formed by adjacent grid cells.* citeturn16search1
6. N-Queens — *Place queens so that none attack one another.* citeturn15search3
7. Sudoku Solver — *Fill a Sudoku board while maintaining validity constraints.* citeturn16search2

## Greedy and Dynamic Programming

### Greedy

1. Jump Game — *Determine whether the last index is reachable.* citeturn5view0
2. Jump Game II — *Minimise the number of jumps needed to reach the end.* citeturn10search2
3. Gas Station — *Find the starting station that allows a full circular tour.* citeturn10search3
4. Task Scheduler — *Minimise CPU intervals while respecting cooldown gaps.* citeturn10search0
5. Hand of Straights — *Check whether cards can be partitioned into consecutive groups.* citeturn10search1
6. Largest Number in One Swap — *Make the best possible number with a single swap.* citeturn5view0

### Dynamic Programming

1. Count Ways to Reach the n’th Stair — *Count valid ways to reach a target step count.* citeturn5view0
2. House Robber — *Maximise loot without robbing adjacent houses.* citeturn14search0
3. Coin Change — *Find the minimum number of coins needed for a given amount.* citeturn5view0
4. Partition Equal Subset Sum — *Decide whether an array can be split into two equal-sum subsets.* citeturn14search1
5. Longest Increasing Subsequence — *Find the longest strictly increasing subsequence.* citeturn5view0
6. Longest Common Subsequence — *Find the longest subsequence common to two strings.* citeturn5view0
7. Word Break — *Determine whether a string can be segmented into dictionary words.* citeturn5view0
8. Decode Ways — *Count the number of ways to decode a digit string.* citeturn14search2
9. Unique Paths with Obstacles — *Count valid grid paths while avoiding blocked cells.* citeturn14search3
10. Edit Distance — *Compute the minimum edits needed to transform one string into another.* citeturn6view0
11. Matrix Chain Multiplication — *Find the minimum-cost parenthesisation of matrix products.* citeturn5view0
12. Egg Dropping Puzzle — *Minimise the worst-case number of trials for critical-floor detection.* citeturn5view0

## Bit Manipulation and Math

### Bit Manipulation

1. Power of Two — *Check whether a number has exactly one set bit.* citeturn6view0
2. Number of 1 Bits — *Count the set bits in a binary representation.* citeturn17search1
3. Missing Number — *Recover the missing value in a \(0 \ldots n\) range.* citeturn6view0
4. Counting Bits — *Return the set-bit count for every number up to \(n\).* citeturn17search2
5. Reverse Bits — *Reverse the bit pattern of an integer.* citeturn6view0
6. Sum of Two Integers — *Add without using the plus or minus operators.* citeturn17search3

### Math and Geometry

1. Plus One — *Simulate increment on a large integer stored as digits.* citeturn18search3
2. Happy Number — *Detect whether repeated square-digit sums converge to 1.* citeturn21search0
3. Pascal Triangle — *Generate the first \(n\) rows of Pascal’s triangle.* citeturn6view0
4. Pow(x, n) — *Implement fast exponentiation correctly for large exponents.* citeturn21search1
5. Factorial of a Large Number — *Compute factorial values that exceed normal integer limits.* citeturn5view0
6. Multiply Strings — *Multiply arbitrarily large integers given as strings.* citeturn8view0